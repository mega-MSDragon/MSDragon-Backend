# AI 실수 재발 방지 기록

이 문서는 하네스 엔지니어링의 일부로, AI가 작업 중 저지른 실수와 재발 방지 규칙을 기록합니다.
새 작업자는 작업 전에 이 문서를 확인하고, 유사한 작업에서 같은 실수를 반복하지 않아야 합니다.

---

## 기록 규칙

- 실수가 발견되면 원인, 영향, 재발 방지 규칙을 짧게 기록합니다.
- 단순 회고가 아니라 다음 작업에서 바로 적용 가능한 체크리스트 형태로 남깁니다.
- 같은 유형의 실수가 반복되면 기존 항목을 보강합니다.

---

## 실수 판단 기준

아래 패턴이 발생하면 실수로 간주하고 이 문서에 기록합니다.

- 에이전트가 코드를 수정하거나 구현 방향을 제안한 뒤, 사용자가 누락이나 판단 오류를 지적한 경우
- 에이전트가 자신의 누락, 오판, 검증 부족을 인정한 경우
- 코드 자체는 동작하더라도 사용자가 기대한 기존 프로젝트 컨벤션, 문서화 수준, 커밋 방식, 검증 절차를 빠뜨린 경우
- 사용자가 직접 수정 기준이나 운영 규칙을 보강해 달라고 요청한 이유가 이전 응답의 누락 때문인 경우

기록하지 않아도 되는 경우:

- 사용자의 선호가 새로 정해져 기존 기준이 바뀐 경우
- 요구사항이 작업 당시 명확히 주어지지 않았고, 에이전트가 합리적인 가정을 명시한 경우
- 단순한 추가 요청이며 이전 작업의 누락이나 오판과 관련 없는 경우

---

## 실수 목록

### 2026-05-09: Swagger 문서화 어노테이션 누락

**상황**

- 기존 Java Spring 프로젝트의 컨트롤러에는 `@Tag`, `@Operation`, `@ApiResponses`가 있었지만, Kotlin 프로젝트로 옮길 때 초기 구현에서 Swagger 설명 어노테이션을 누락했습니다.
- 사용자가 Swagger 설명과 주석 누락을 지적한 뒤에야 보완했습니다.

**원인**

- API 동작 구현과 테스트 통과에만 집중했고, 기존 프로젝트의 문서화/주석 관례를 구현 완료 조건에 포함하지 않았습니다.
- Swagger 설정 파일은 추가했지만, 각 API 엔드포인트의 Swagger 설명까지 포함해야 한다는 요구를 암묵적 컨벤션으로 인식하지 못했습니다.

**영향**

- Swagger UI에서 API 목적, 파라미터, 응답 코드 설명이 부족해져 API 문서 품질이 떨어졌습니다.
- 사용자가 기대한 기존 Spring 프로젝트의 개발 스타일과 결과물이 달라졌습니다.

**재발 방지 규칙**

- 컨트롤러를 추가하거나 수정할 때는 반드시 Swagger 문서화 어노테이션을 함께 작성합니다.
- 기본 체크리스트:
  - 컨트롤러 클래스에 `@Tag`
  - 각 엔드포인트에 `@Operation`
  - 성공/실패 응답에 `@ApiResponses`
  - 요청 파라미터와 path variable에 필요한 경우 `@Parameter`
  - 요청/응답 DTO 필드에 `@Schema`
- 사용자가 별도로 "Swagger 주석"을 말하지 않아도 API 작업에는 Swagger 문서화를 포함합니다.
- Kotlin에서 프로젝트의 `ApiResponse` 클래스와 Swagger의 `ApiResponse` 어노테이션 이름이 겹치므로 Swagger 어노테이션은 alias import를 사용합니다.

```kotlin
import io.swagger.v3.oas.annotations.responses.ApiResponse as SwaggerApiResponse
```

**검증 방법**

- `./gradlew test`로 컴파일을 확인합니다.
- 애플리케이션 실행 후 `/swagger-ui.html`에서 태그, 요약, 설명, 응답 코드가 보이는지 확인합니다.

### 2026-07-06: 와이어프레임 예시를 기능 제약으로 오해

**상황**

- 여행 날짜 선택 화면의 예시 문구와 화면 구성만 보고 여행 기간을 당일치기/1박 2일로 제한하는 로직을 추가했습니다.
- 사용자가 해당 문구는 제약이 아니며 여행 기간 제한이 없다고 정정했습니다.

**원인**

- 와이어프레임에 명시된 확정 설명과 예시 UI 상태를 구분하지 않았습니다.
- 애매한 기획은 질문하라는 사용자 운영 규칙이 있었지만, 화면에 보이는 선택지만 근거로 서버 제약을 임의 확정했습니다.

**재발 방지 규칙**

- 와이어프레임의 예시 데이터, 선택된 상태, 샘플 문구를 서버 제약으로 확정하지 않습니다.
- DB 제약, API 검증, enum 값처럼 되돌리기 어려운 설계는 화면 설명에 명시되어 있거나 사용자가 확정한 경우에만 반영합니다.
- 확정 근거가 "화면에 그렇게 보인다" 수준이면 구현 전에 질문합니다.

### 2026-07-07: 환경변수 추가 시 `.env.example` 갱신 누락

**상황**

- TMAP 경로 최적화 설정을 `application.yaml`과 로컬 실행 문서에는 추가했지만, 새 환경변수를 `.env.example`에 추가하지 않았습니다.
- 사용자가 예시 env 파일 누락을 지적한 뒤에야 보완했습니다.

**원인**

- 설정 구현과 문서 설명만 확인했고, 실제 개발자가 복사해 쓰는 환경변수 템플릿을 완료 조건에 포함하지 않았습니다.

**재발 방지 규칙**

- 새 환경변수를 추가하거나 이름을 바꿀 때는 반드시 아래 항목을 함께 확인합니다.
  - `application*.yaml` 또는 설정 클래스
  - `.env.example`
  - `docs/setup/local-run.md`
  - 배포/운영 환경변수 문서가 있다면 해당 문서
- 추적 파일에는 실제 키나 시크릿을 넣지 않고 빈 값 또는 안전한 placeholder만 기록합니다.

### 2026-07-14: Docker Compose 환경변수 전달 누락

**상황**

- TMAP 설정을 `application.yaml`과 `.env.example`에는 추가했지만, `docker-compose.yml`의 app container environment에 전달하지 않았습니다.
- 운영 서버 `.env`에 값이 있어도 컨테이너가 값을 받지 못해 경로 최적화 API가 "Tmap 앱키 설정이 완료되지 않았습니다." 오류로 실패했습니다.

**원인**

- Spring 설정 바인딩과 env 템플릿만 확인했고, Docker Compose가 명시적으로 컨테이너에 넘기는 환경변수 목록을 완료 조건에 포함하지 않았습니다.

**재발 방지 규칙**

- 새 환경변수를 추가하거나 이름을 바꿀 때는 반드시 아래 항목을 한 세트로 확인합니다.
  - `application*.yaml` 또는 설정 클래스
  - `.env.example`
  - `docker-compose.yml`의 app `environment`
  - `docs/setup/local-run.md`
  - `docs/setup/deploy.md`
  - 운영 서버 `.env`
- 실제 secret 값은 Git, 문서, 대화 요약에 남기지 않고 placeholder로만 기록합니다.

### 2026-07-15: Kotlin 기본 생성자 프로퍼티에 setter 가시성 지정 반복

**상황**

- JPA Entity 기본 생성자의 `var` 파라미터 바로 다음 줄에 `protected set`을 작성해 Kotlin 컴파일 오류가 발생했습니다.
- 여행 Entity 수정 작업에서 이미 한 번 발견하고 고쳤던 문법 오류를 새 Entity 작성 중 다시 반복했습니다.

**원인**

- 클래스 본문 프로퍼티에 사용할 수 있는 setter 가시성 문법을 기본 생성자 프로퍼티에도 그대로 적용했습니다.

**재발 방지 규칙**

- Kotlin 기본 생성자에 선언한 `var` 프로퍼티에는 `private set`, `protected set`을 별도 줄로 붙이지 않습니다.
- setter 가시성 제한이 반드시 필요하면 프로퍼티를 클래스 본문으로 옮기고, JPA 생성 요구사항과 테스트를 함께 확인합니다.
- 이 프로젝트의 단순 JPA Entity는 기존 패턴처럼 생성자 `var`를 유지하고 상태 변경 의도는 도메인 메서드로 표현합니다.
- 새 Entity 여러 개를 만든 직후에는 API 구현 전에 `./gradlew compileKotlin`을 먼저 실행합니다.

### 2026-07-15: 확정한 참여자 변경 후 재서명 정책 구현 누락

**상황**

- 참여 부모 구성이 바뀌면 여행 10계명을 다시 작성하고 서명하기로 사용자와 확정했지만, 여행 수정 기능 구현 시 코스와 참여자만 갱신하고 기존 10계명과 서명을 유지했습니다.
- 사용자가 이전 결정이 적용되지 않았다고 지적한 뒤 초기화 로직을 추가했습니다.

**원인**

- 여행 수정과 10계명 구현을 서로 다른 작업으로 진행하면서 두 도메인의 상태 전이 조건을 연결하지 않았습니다.
- 대화에서 확정한 정책을 관련 정책 문서와 통합 테스트에 즉시 반영하지 않아 후속 구현의 완료 조건에서 빠졌습니다.

**재발 방지 규칙**

- 참여자, 권한 주체, 소유자가 바뀌는 기능을 수정할 때는 관련 도메인의 서명, 동의, 승인 데이터를 유지할 수 있는지 함께 확인합니다.
- 사용자와 정책을 확정한 즉시 관련 `docs/policy`, API 문서, 도메인 문서에 기록하고 교차 도메인 부작용을 통합 테스트로 고정합니다.
- 여행 참여 부모 변경 시에는 코스 초기화뿐 아니라 10계명 확정본과 모든 서명 초기화 여부를 체크리스트로 확인합니다.

### 2026-07-15: OS 시계와 DB 시간 정밀도 차이로 CI 테스트 실패

**상황**

- 첫 부모 서명 응답의 `completedAt`과 다음 부모 서명 응답의 `completedAt`이 같아야 하는 테스트가 macOS 로컬에서는 통과했지만 Linux CI에서 실패했습니다.
- 첫 응답은 `LocalDateTime.now()`의 나노초 값을 사용하고, 다음 응답은 DB `timestamp(6)`를 거친 마이크로초 값을 사용해 문자열이 달라졌습니다.

**원인**

- 운영 DB가 보존하는 시간 정밀도와 애플리케이션에서 즉시 반환하는 시간 정밀도를 맞추지 않았습니다.
- 로컬 OS의 현재 시각 정밀도에서는 우연히 차이가 드러나지 않아 전체 테스트 1회 통과만으로 CI 재현 가능성을 놓쳤습니다.

**재발 방지 규칙**

- 저장 전 생성한 시간을 같은 요청에서 즉시 응답할 때는 DB 컬럼 정밀도에 맞춰 먼저 정규화합니다.
- PostgreSQL/H2 `timestamp(6)`에 저장하는 현재 시각은 `ChronoUnit.MICROS`로 절삭한 뒤 Entity와 응답에 함께 사용합니다.
- 저장 직후 응답과 다음 요청의 재조회 응답이 같아야 하는 시간 필드는 DB 왕복 전후 동일성 테스트를 작성합니다.

### 2026-07-15: PDF 요청의 오류 응답 미디어 타입 누락

**상황**

- PDF API의 완료 조건 검증은 `BadRequestException`을 던졌지만, Swagger 요청의 `Accept: application/pdf`와 공통 JSON 예외 응답이 충돌해 운영 서버에서는 `400`이 아닌 `500`으로 처리됐습니다.
- 기존 통합 테스트가 실제 Swagger 요청과 달리 `Accept` 헤더를 보내지 않아 문제를 발견하지 못했습니다.

**원인**

- `ControllerExceptionAdvice`가 오류 응답의 `Content-Type: application/json`을 명시하지 않아 Spring이 클라이언트가 요청한 PDF 타입으로 `ApiResponse`를 직렬화하려 했습니다.
- 바이너리 성공 응답과 JSON 오류 응답을 함께 사용하는 엔드포인트의 콘텐츠 협상 조건을 테스트하지 않았습니다.

**재발 방지 규칙**

- 공통 예외 처리기는 모든 오류 응답의 `Content-Type`을 `application/json`으로 명시합니다.
- PDF, 이미지, 파일 다운로드 API의 오류 통합 테스트는 실제 성공 미디어 타입을 `Accept` 헤더로 보내고도 상태 코드와 JSON 오류 본문이 유지되는지 확인합니다.
- Swagger에 표시되는 요청 헤더를 운영 재현 조건으로 포함합니다.

### 2026-08-03: HTTP 200 응답 계약에서 Validation과 인증 실패 누락

**상황**

- 정책 오류를 HTTP `200`으로 통일하는 작업에서 Validation 실패는 HTTP `400`, Bearer access token 인증 실패는 HTTP `401`로 남겨두었습니다.
- 사용자가 서버가 실패 결과를 정상적으로 만들어 반환하는 경우는 모두 HTTP `200`이어야 한다고 정정했습니다.

**원인**

- 일반적인 REST HTTP 상태 관례를 프로젝트에서 합의한 클라이언트 계약보다 우선했습니다.
- "기본 HTTP 오류"와 서버가 처리한 요청·인증 실패의 경계를 너무 넓게 해석했습니다.

**재발 방지 규칙**

- 성공, JSON·파라미터·Validation 오류, access token 인증 실패, 도메인 정책 오류는 모두 실제 HTTP `200`으로 반환합니다.
- 클라이언트는 본문 `success`와 `status`로 성공·실패를 분기합니다.
- 존재하지 않는 URL, 지원하지 않는 method·media type 등 라우팅/프로토콜 오류와 예상하지 못한 서버·외부 연동 실패만 실제 HTTP 오류를 사용합니다.
- 새 API의 Swagger와 테스트에는 실제 HTTP `200`과 본문 `status`를 따로 검증합니다.

### 2026-08-06: Swagger 예시와 기본 미디어 타입 분리

**상황**

- 내부 `status`별 예시를 `application/json`에 추가했지만 Springdoc이 성공 응답 schema를 `*/*`에 생성해 Swagger UI의 기본 화면에는 예시가 보이지 않았습니다.
- `/v3/api-docs`에서 예시 경로만 검사하고 Swagger UI가 기본 선택하는 media type까지 확인하지 않아 사용자가 운영 Swagger에서 다시 문제를 발견했습니다.

**원인**

- OpenAPI JSON에 `examples`가 존재하는지만 검증하고 schema와 examples가 같은 media type에 있는지 확인하지 않았습니다.

**재발 방지 규칙**

- Swagger 응답 예시를 추가할 때 schema와 examples가 같은 media type에 있는지 검증합니다.
- 공통 JSON 응답의 `*/*` schema는 `application/json`으로 정규화합니다.
- 바이너리 응답은 성공 media type을 유지하고 JSON 오류 예시에만 정규화를 적용합니다.
- OpenAPI 테스트는 `application/json.schema`, `application/json.examples`, 불필요한 `*/*` 제거를 함께 확인합니다.
