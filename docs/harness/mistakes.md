# AI 실수 재발 방지 기록

이 문서는 하네스 엔지니어링의 일부로, AI가 작업 중 저지른 실수와 재발 방지 규칙을 기록합니다.
새 작업자는 작업 전에 이 문서를 확인하고, 유사한 작업에서 같은 실수를 반복하지 않아야 합니다.

---

## 기록 규칙

- 실수가 발견되면 원인, 영향, 재발 방지 규칙을 짧게 기록합니다.
- 단순 회고가 아니라 다음 작업에서 바로 적용 가능한 체크리스트 형태로 남깁니다.
- 같은 유형의 실수가 반복되면 기존 항목을 보강합니다.

---

## 실수 판단 기준

아래 패턴이 발생하면 실수로 간주하고 이 문서에 기록합니다.

- 에이전트가 코드를 수정하거나 구현 방향을 제안한 뒤, 사용자가 누락이나 판단 오류를 지적한 경우
- 에이전트가 자신의 누락, 오판, 검증 부족을 인정한 경우
- 코드 자체는 동작하더라도 사용자가 기대한 기존 프로젝트 컨벤션, 문서화 수준, 커밋 방식, 검증 절차를 빠뜨린 경우
- 사용자가 직접 수정 기준이나 운영 규칙을 보강해 달라고 요청한 이유가 이전 응답의 누락 때문인 경우

기록하지 않아도 되는 경우:

- 사용자의 선호가 새로 정해져 기존 기준이 바뀐 경우
- 요구사항이 작업 당시 명확히 주어지지 않았고, 에이전트가 합리적인 가정을 명시한 경우
- 단순한 추가 요청이며 이전 작업의 누락이나 오판과 관련 없는 경우

---

## 실수 목록

### 2026-05-09: Swagger 문서화 어노테이션 누락

**상황**

- 기존 Java Spring 프로젝트의 컨트롤러에는 `@Tag`, `@Operation`, `@ApiResponses`가 있었지만, Kotlin 프로젝트로 옮길 때 초기 구현에서 Swagger 설명 어노테이션을 누락했습니다.
- 사용자가 Swagger 설명과 주석 누락을 지적한 뒤에야 보완했습니다.

**원인**

- API 동작 구현과 테스트 통과에만 집중했고, 기존 프로젝트의 문서화/주석 관례를 구현 완료 조건에 포함하지 않았습니다.
- Swagger 설정 파일은 추가했지만, 각 API 엔드포인트의 Swagger 설명까지 포함해야 한다는 요구를 암묵적 컨벤션으로 인식하지 못했습니다.

**영향**

- Swagger UI에서 API 목적, 파라미터, 응답 코드 설명이 부족해져 API 문서 품질이 떨어졌습니다.
- 사용자가 기대한 기존 Spring 프로젝트의 개발 스타일과 결과물이 달라졌습니다.

**재발 방지 규칙**

- 컨트롤러를 추가하거나 수정할 때는 반드시 Swagger 문서화 어노테이션을 함께 작성합니다.
- 기본 체크리스트:
  - 컨트롤러 클래스에 `@Tag`
  - 각 엔드포인트에 `@Operation`
  - 성공/실패 응답에 `@ApiResponses`
  - 요청 파라미터와 path variable에 필요한 경우 `@Parameter`
  - 요청/응답 DTO 필드에 `@Schema`
- 사용자가 별도로 "Swagger 주석"을 말하지 않아도 API 작업에는 Swagger 문서화를 포함합니다.
- Kotlin에서 프로젝트의 `ApiResponse` 클래스와 Swagger의 `ApiResponse` 어노테이션 이름이 겹치므로 Swagger 어노테이션은 alias import를 사용합니다.

```kotlin
import io.swagger.v3.oas.annotations.responses.ApiResponse as SwaggerApiResponse
```

**검증 방법**

- `./gradlew test`로 컴파일을 확인합니다.
- 애플리케이션 실행 후 `/swagger-ui.html`에서 태그, 요약, 설명, 응답 코드가 보이는지 확인합니다.

### 2026-07-06: 와이어프레임 예시를 기능 제약으로 오해

**상황**

- 여행 날짜 선택 화면의 예시 문구와 화면 구성만 보고 여행 기간을 당일치기/1박 2일로 제한하는 로직을 추가했습니다.
- 사용자가 해당 문구는 제약이 아니며 여행 기간 제한이 없다고 정정했습니다.

**원인**

- 와이어프레임에 명시된 확정 설명과 예시 UI 상태를 구분하지 않았습니다.
- 애매한 기획은 질문하라는 사용자 운영 규칙이 있었지만, 화면에 보이는 선택지만 근거로 서버 제약을 임의 확정했습니다.

**재발 방지 규칙**

- 와이어프레임의 예시 데이터, 선택된 상태, 샘플 문구를 서버 제약으로 확정하지 않습니다.
- DB 제약, API 검증, enum 값처럼 되돌리기 어려운 설계는 화면 설명에 명시되어 있거나 사용자가 확정한 경우에만 반영합니다.
- 확정 근거가 "화면에 그렇게 보인다" 수준이면 구현 전에 질문합니다.
